# 导入streamlit库，约定俗成简称为st
import streamlit as st
st.set_page_config(
    page_title="原神牛逼",  # 这里改你想要的网页名称
    page_icon="./image/ciallo.jpg",  # 可选：网页图标（可以是emoji或本地图片路径）
)
# 1. 页面标题/文本类组件（展示信息）
st.title("千恋万花")  # 主标题
st.subheader("魔女的夜宴")  # 子标题
st.write("Ciallo～ (∠・ω< )⌒★")

# 2. 交互类组件（获取用户输入）
name = st.text_input("君の名は：")  # 文本输入框
age = st.slider("请选择你的年龄：", 0, 114514, 20)  # 滑块（最小值，最大值，默认值）

# 3. 逻辑处理 + 结果展示
if st.button("原神启动"):  # 按钮组件
    st.success(f"Ciallo～ (∠・ω< )⌒★，{name}Senpai！你的年龄是{age}岁，欢迎游玩《千恋万花》~ ！")
    # 展示数据示例（比如一个简单的DataFrame）
    import pandas as pd
    df = pd.DataFrame({
        "姓名": [name],
        "年龄": [age],
        "爱好": [f"巡ちゃん～和玩原神"],
        "身份": ["雑魚柚子厨~💗💗💗"]
    })
    st.dataframe(df)  # 展示表格